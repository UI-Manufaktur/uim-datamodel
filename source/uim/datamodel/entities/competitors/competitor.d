module uim.datamodel.entities.Competitory.Competitor;

import uim.datamodel;

@safe class DMODCompetitor : DMODEntity {
  // mixin(EntityThis!());
}
/*
auto BUSCompetitor() { return new DMODCompetitor; }
auto BUSCompetitor(UUID newId, string newName) { return new DMODCompetitor(newId, newName); }
unittest {
  // writeln();
}

@path(restPath)
interface IBUSCompetitors {
  mixin(IEIEntitiesRest!("Competitor", "Competitors"));
}

@safe class DMODCompetitors : IBUSCompetitors {
  mixin(EntitiesThis!());
  mixin(OEntitiesInner!("Competitor", "Competitors"));
  mixin(OEntitiesRest!("Competitor", "Competitors"));
}
*/